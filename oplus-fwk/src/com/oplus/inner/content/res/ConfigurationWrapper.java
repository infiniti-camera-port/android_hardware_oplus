package com.oplus.inner.content.res;

/**
 * Boot-classpath stub of the OEM class {@code com.oplus.inner.content.res.ConfigurationWrapper}.
 *
 * OEM inner-API reflection wrapper over an AOSP framework type. Hard-loaded by the shipped OppoGallery2 / OplusCamera (type / const-class reference); absent from our
 * LOS oplus-fwk it raises NoClassDefFoundError — an uncaught java.lang.Error — crashing the app on launch / disabling the feature. Stubbed to close the
 * linkage; members return safe defaults (false / 0 / null / empty / no-op) as only the type
 * surface is consumed here.
 */
public class ConfigurationWrapper {
}
