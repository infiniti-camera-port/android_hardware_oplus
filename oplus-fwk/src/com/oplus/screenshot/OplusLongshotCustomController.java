package com.oplus.screenshot;

/**
 * Boot-classpath stub of the OEM class {@code com.oplus.screenshot.OplusLongshotCustomController}.
 *
 * OEM com.oplus.screenshot framework surface. Hard-loaded by OppoGallery2 (type / const-class reference); absent from our
 * LOS oplus-fwk it raises NoClassDefFoundError — an uncaught java.lang.Error — crashing the app on launch / disabling the feature. Stubbed to close the
 * linkage; members return safe defaults (false / 0 / null / empty / no-op) as only the type
 * surface is consumed here.
 */
public class OplusLongshotCustomController {

    public OplusLongshotCustomController(OplusLongshotViewBase view, String source) {}

    public boolean findInfo(com.oplus.screenshot.OplusLongshotViewInfo info) {
        return false;
    }

    public void onLongScroll() {
    }
}
