package com.oplus.android.internal.util;

/**
 * Boot-classpath stub of the OEM class {@code com.oplus.android.internal.util.OplusFrameworkStatsLog}.
 *
 * OEM com.oplus.android framework surface. Hard-loaded by the shipped OppoGallery2 / OplusCamera (type / const-class reference); absent from our
 * LOS oplus-fwk it raises NoClassDefFoundError — an uncaught java.lang.Error — crashing the app on launch / disabling the feature. Stubbed to close the
 * linkage; members return safe defaults (false / 0 / null / empty / no-op) as only the type
 * surface is consumed here.
 */
public class OplusFrameworkStatsLog {
}
