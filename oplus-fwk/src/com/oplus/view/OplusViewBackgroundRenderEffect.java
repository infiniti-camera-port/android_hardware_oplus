package com.oplus.view;

import android.graphics.RenderEffect;
import android.view.View;

/* Closure stub. Only the static setter referenced by OppoGallery2 is
 * provided; no-op so absence of the OEM background-render-effect path
 * degrades gracefully. */
/**
 * Boot-classpath stub of the OEM class {@code com.oplus.view.OplusViewBackgroundRenderEffect}.
 *
 * OEM graphics / view rendering extension. Hard-loaded by OppoGallery2 (type / const-class reference); absent from our
 * LOS oplus-fwk it raises NoClassDefFoundError — an uncaught java.lang.Error — crashing the app on launch / disabling the feature. Stubbed to close the
 * linkage; members return safe defaults (false / 0 / null / empty / no-op) as only the type
 * surface is consumed here.
 */
public class OplusViewBackgroundRenderEffect {

    private OplusViewBackgroundRenderEffect() {
    }

    public static void setBackgroundRenderEffect(RenderEffect renderEffect, View view) {
    }
}
