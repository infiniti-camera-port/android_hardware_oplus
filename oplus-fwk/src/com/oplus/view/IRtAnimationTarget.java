package com.oplus.view;

/* Closure stub. Referenced by OppoGallery2 only as a parameter type of
 * OplusRenderNodeAnimator.createRtAnimator(IRtAnimationTarget, View). No
 * members are referenced, so a plain empty interface is sufficient. The OEM
 * type extends com.oplus.view.ITarget, but nothing in the shipped consumers
 * references ITarget, so it is intentionally not pulled in. */
public interface IRtAnimationTarget {
}
