package com.oplus.view;

import android.content.Context;
import android.view.WindowInsetsController;

/* Closure stub. Only the static helper referenced by OppoGallery2 is
 * provided; no-op. Declared final to match the OEM class kind. The OEM
 * type implements com.oplus.view.IOplusWindowManagerConstans, but no shipped
 * consumer references that interface, so it is intentionally omitted. */
/**
 * Boot-classpath stub of the OEM class {@code com.oplus.view.OplusWindowManager}.
 *
 * OEM graphics / view rendering extension. Hard-loaded by OppoGallery2 (type / const-class reference); absent from our
 * LOS oplus-fwk it raises NoClassDefFoundError — an uncaught java.lang.Error — crashing the app on launch / disabling the feature. Stubbed to close the
 * linkage; members return safe defaults (false / 0 / null / empty / no-op) as only the type
 * surface is consumed here.
 */
public final class OplusWindowManager {

    public static void setUseInsetsAnimationThread(Context context,
            WindowInsetsController controller, boolean useThread) {
    }
}
