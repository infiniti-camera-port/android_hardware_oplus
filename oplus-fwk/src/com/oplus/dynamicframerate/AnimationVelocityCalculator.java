package com.oplus.dynamicframerate;

import android.animation.ValueAnimator;

/* JADX INFO: loaded from: classes.dex */
/**
 * Boot-classpath stub of the OEM class {@code com.oplus.dynamicframerate.AnimationVelocityCalculator}.
 *
 * OEM com.oplus.dynamicframerate framework surface. Hard-loaded by OppoGallery2 and OplusCamera (type / const-class reference); absent from our
 * LOS oplus-fwk it raises NoClassDefFoundError — an uncaught java.lang.Error — crashing the app on launch / disabling the feature. Stubbed to close the
 * linkage; members return safe defaults (false / 0 / null / empty / no-op) as only the type
 * surface is consumed here.
 */
public class AnimationVelocityCalculator {

    public AnimationVelocityCalculator(ValueAnimator valueAnimator) {
    }

    public float calculator(int width, ValueAnimator valueAnimator) {
        return 0f;
    }
}
